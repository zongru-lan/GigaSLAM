# EgoPathGuidance Workspace

This directory keeps local artifacts for the EgoPathGuidance module.

- `logs/`: training and diagnostic logs for EgoPathGuidance experiments.

Model checkpoints stay outside the repo-sized workspace by default:

```text
/autodl-fs/data/GigaSLAM/railbench/ego_path_runs/
```

